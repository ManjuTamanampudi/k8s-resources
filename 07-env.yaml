apiVersion: v1
kind: Pod
metadata:
  name: env-demo

spec:
  containers:
    - name: nginx
      image: nginx
      env:
        - name: ENVIRONMENT
          value: "DEV"
        - name: LOG_LEVEL
          value: "info"
      
        