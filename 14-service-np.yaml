apiVersion: v1
kind: Service
metadata:
  name: service-np-demo 
spec:
  type: NodePort
  selector:
     app: roboshop
     environment: dev
     tier: frontend

  ports:
    - protocol: TCP 
      port: 80
      targetPort: 80  
      nodePort: 30080

  