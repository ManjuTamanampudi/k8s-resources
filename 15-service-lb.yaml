apiVersion: v1
kind: Service
metadata:
  name: service-lb-demo 
spec:
  type: LoadBalancer
  selector:
     app: roboshop
     environment: dev
     tier: frontend

  ports:
    - protocol: TCP 
      port: 80 #service port
      targetPort: 80 #container port 

  