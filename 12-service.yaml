apiVersion: v1
kind: Service
metadata:
  name: nginx 
spec:
  type: ClusterIP
  selector:   
     app: roboshop
     environment: dev
     tier: backend
     service: db

  ports:
    - protocol: TCP 
      port: 80
      targetPort: 80  
      # nodePort: 30080

  